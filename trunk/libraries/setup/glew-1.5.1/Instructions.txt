Instructions for Linux (revise when back in Windows);

1) Extract tgz.
2) Rename glew subdirectory to glew-1.5.1.
3) Run 'fromdos config/config.guess' in glew-1.5.1 directory to convert line endings.
4) Make sure the development files for libxmu are installed.
5) Run 'make'.

